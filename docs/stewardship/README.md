# Living-intention ledger

Implements the preservation surface of #56 using public inventory #47, not a new inventory claim.

- `inventory-2026-09-13.md` is the verbatim planning-era snapshot. Its action boundary and statuses describe that dated pass; current owner authorization and later events are recorded separately.
- `intentions.json` contains all 25 unique intention/family cards and successor issue routes. Each family retains its member list and source evidence. `unknown_coverage` records sources that have not been inspected.
- `execution-events.jsonl` appends implementation receipts. A merge event is not a family closure or a claim that a whole epic is complete.

Never rewrite the snapshot to make today's tracker look cleaner. Append a sourced event with issue, PR, exact head/merge receipt, disposition and remaining evidence. Before any relocation, checksum source files and account for every unique member's residue; no migration/deletion is authorized by this ledger.

The old API/SDK/SaaS prescriptions remain parked purpose-drift evidence; they are not an instruction to create a new product. Generated agent captures remain preserved until removed instructions and unique rationale are reconciled. Current roadmap is issue #10, not a competing chronological to-do list.

External private records and unpushed local work remain explicitly unknown. This repository contains only public source references and sanitized decisions. #56 stays open until those source owners and donor comparisons supply the missing evidence.
